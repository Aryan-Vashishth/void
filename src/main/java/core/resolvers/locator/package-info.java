/**
 * Locator resolution layer.
 *
 * <p>Reads element locators from external sources (JSON / properties) and
 * resolves them into Selenium {@code By} instances at runtime. The format-specific
 * readers live in the {@code json} and {@code properties} sub-packages.</p>
 */
package core.resolvers.locator;

