/**
 * Locator-template formatting.
 *
 * <p>{@link core.resolvers.locator.template.LocatorTemplate} is a value object wrapping a
 * raw template string with a configurable {@link core.resolvers.locator.template.LocatorTemplate.Policy}:
 * {@code STRICT} (indexed {@code %s}/{@code %n$s}, throws on too few args) or
 * {@code PAD_LAST} (case-insensitive, repeats last arg).</p>
 */
package core.resolvers.locator.template;

