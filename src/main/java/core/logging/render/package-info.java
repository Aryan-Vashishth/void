/**
 * Rendering pipeline. {@link core.logging.render.LogActions} owns every action
 * method (click, table, success, …) and the multi-line ANSI composition that
 * dispatches each rendered line to Log4j.
 */
package core.logging.render;

