/**
 * Runtime configuration and the Log4j logger holder.
 *
 * <ul>
 *   <li>{@link core.logging.config.LogConfig}     — single source of truth for all mutable settings</li>
 *   <li>{@link core.logging.config.LoggerContext} — Log4j logger handle + backward-compat delegates</li>
 * </ul>
 */
package core.logging.config;

