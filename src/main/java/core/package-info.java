/**
 * Core framework infrastructure — driver management, logging, locator resolution,
 * and general-purpose utilities.
 *
 * <ul>
 *   <li>{@code core.driver}           — WebDriver lifecycle ({@code DriverFactory}, {@code DriverContext})</li>
 *   <li>{@code core.logging}          — ANSI-colored, theme-aware logger ({@code CustomLogger})</li>
 *   <li>{@code core.resolvers.locator} — Role-based locator resolution pipeline</li>
 *   <li>{@code core.utils}            — Configuration, DOM helpers, data utilities, I/O</li>
 * </ul>
 */
package core;

