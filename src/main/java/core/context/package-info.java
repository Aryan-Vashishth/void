/**
 * {@code core.context} — Per-session execution context.
 *
 * <p>Contains {@link core.context.ExecutionContext}, the immutable holder for
 * a session's resolved configuration and WebDriver instance. Replaces hidden
 * global state with explicit dependency passing.</p>
 */
package core.context;

