/**
 * Cross-cutting framework utilities — configuration, enum resolution, UI context,
 * and the legacy {@code CustomLogger} shim.
 *
 * <p>For the canonical logger see {@link core.logging.CustomLogger}.</p>
 */
package core.utils;

