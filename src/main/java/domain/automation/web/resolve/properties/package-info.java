/**
 * {@code domain.automation.web.resolve.properties} -- Properties-format locator file reader.
 *
 * <p>Reads locator values from {@code .properties}-format locator files, which use
 * standard Java properties syntax ({@code KEY=xpath=//button}).</p>
 *
 * <h3>Key types</h3>
 * <ul>
 *   <li>{@link domain.automation.web.resolve.properties.PropertiesFileLocatorReader} -- reads
 *       a classpath-relative properties file and resolves a keyed locator string.</li>
 * </ul>
 */
package domain.automation.web.resolve.properties;
