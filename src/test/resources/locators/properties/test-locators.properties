# -------------------------------------------------------
# Test fixture: locators/test-locators.properties
# Used by PropertiesFileLocatorReaderV1Test and LocatorResolverV1Test
# -------------------------------------------------------

# XPath (raw ? no prefix)
BUTTON_TRIGGER = //button[@id='submit']

# CSS (explicit prefix)
LOGIN_INPUT = css=input#username

# ID (explicit prefix)
SUBMIT_BUTTON = id=submitBtn

# Name (explicit prefix)
NAME_ELEMENT = name=searchField

# Class (explicit prefix)
CLASS_ELEMENT = class=active-item

# Tag (explicit prefix)
TAG_ELEMENT = tag=button

# LinkText (explicit prefix)
LINK_TEXT = linkText=Click here

# PartialLinkText (explicit prefix)
PARTIAL_LINK = partialLinkText=Read more

# XPath (explicit prefix)
XPATH_ELEMENT = xpath=//div[@class='container']

# Templates
TEMPLATE_WITH_ARG = //input[@placeholder='%s']
TEMPLATE_TWO_ARGS = //tr[@data-row='%s']//td[@data-col='%s']

